import { jest, expect, describe, test } from '@jest/globals';

describe('#routes', () => {
  test('my test', () => {
    expect(true).toBeTruthy()
  })
  test('my test 2', () => {
    expect(2).toBe(1 + 1)
  })
})